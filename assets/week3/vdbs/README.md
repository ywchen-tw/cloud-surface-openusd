# Week 3 VDB Placeholder

Week 3 currently uses a structured `UsdGeom.Points` voxel placeholder in `../clouds_vol.usda`.

When OpenVDB/EaR3T export is added, write real `.vdb` files here with grids such as:

- `density`
- `scatter`
- `absorption`
- `radiance_r`
- `radiance_g`
- `radiance_b`
